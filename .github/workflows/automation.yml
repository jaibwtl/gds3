name: CI
on:
  push:
    branches: [ "automation" ]
  pull_request:
    branches: [ "automation" ]
jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Hello, world!
